# Minimal echo backend — returns 200 with request info for any path.
# ModSecurity sits in front and blocks before this gets hit.
server {
    listen 8888;
    server_name localhost;

    location / {
        return 200 '{"status":"ok","path":"$request_uri"}';
        add_header Content-Type application/json;
    }
}
